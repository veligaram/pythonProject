#nested functions=function calls inside the other function calls
#innermost function calls are resolved first
#returned value is used as arguments
print(round(abs(float(input("enter a whole positive number:")))))
